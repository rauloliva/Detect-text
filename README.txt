In order to use the Text-Dectector API
you need to execute this command in a powershell window

* $env:GOOGLE_APPLICATION_CREDENTIALS="credentials.json"

use the following command to execute the setup.ps1 file
* powershell -ExecutionPolicy Bypass -File .\setup.ps1